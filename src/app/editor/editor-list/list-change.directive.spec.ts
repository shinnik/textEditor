import { ListChangeDirective } from './list-change.directive';

describe('ListChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new ListChangeDirective();
    expect(directive).toBeTruthy();
  });
});
