import { BlockHostDirective } from './block-host.directive';

describe('BlockHostDirective', () => {
  it('should create an instance', () => {
    const directive = new BlockHostDirective();
    expect(directive).toBeTruthy();
  });
});
